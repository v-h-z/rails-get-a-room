<!-- <h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p> -->
<!-- <div class="container"> -->
  <div class="search">
    <%= render 'search_with_value' %>
  </div>
  <div class="rooms-container">
    <div class="rooms-list">
      <h3><%= pluralize(@rooms.size, 'salle') %> dans ce secteur</h3>
      <ul class="list-unstyled">
      <% @rooms.each do |room| %>
        <li><%= link_to room.name, room_path(room)  %></li>
      <% end %>
      </ul>
    </div>
    <div id="map"
         style="width: 100%; height: 600px;"
         data-markers="<%= @markers.to_json %>"
         data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
<!-- </div> -->
